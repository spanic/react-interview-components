import {Meta, Canvas, ArgTypes} from '@storybook/blocks';
import * as OfferStories from './Offer.stories';

<Meta of={OfferStories} />

# Offer component (TypeScript! 🎉)

## Description

`Offer` component card displays Offer data: `title`, `description` and `price`. User can add or remove Offer from the order by pressing the corresponding button on the card.

## Props

<ArgTypes of={OfferStories} />

## Demo

<Canvas of={OfferStories.Default} />
