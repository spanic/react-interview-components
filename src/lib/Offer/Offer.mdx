import {Meta, Canvas} from '@storybook/blocks';
import * as OfferStories from './Offer.stories';

<Meta of={OfferStories} />

# Offer component

## Description

`Offer` component is a card that displays Offer data: `title`, `description` and `price`. User can add or remove the Offer from the order by pressing the corresponding button on the card.

## Demo

<Canvas of={OfferStories.Primary} />
